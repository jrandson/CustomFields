
    <%= form_for(text_field) do |f| %>
  <% if text_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(text_field.errors.count, "error") %> prohibited this text_field from being saved:</h2>

      <ul>
      <% text_field.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, 'Text field name', class: 'form-control' %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit "Create", class: 'btn btn-default' %>
  </div>
  
    <% end %>

